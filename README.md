# Avro Viewer (JavaFX)

Desktop UI tool for viewing and filtering Avro files.

## Requirements
- Java 25+

## Commands

`mvn clean install`

`mvn -pl impl -q javafx:run`

`mvn -pl impl -am clean package`
